name: CI/CD Pipeline

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ============================================
  # PR Checks - Run on pull requests only
  # ============================================
  pr_checks:
    name: PR Checks
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Frontend
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci --legacy-peer-deps

      - name: Frontend build, test, typecheck
        working-directory: ./frontend
        run: |
          npm run build
          npm test
          npx tsc --noEmit

      # Server
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      - name: Server build and test
        run: |
          mvn -B test --file server/pom.xml
          mvn -B -DskipTests package --file server/pom.xml

      # Agent
      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: agent/go.mod
          cache-dependency-path: agent/go.sum

      - name: Agent build, test, vet
        working-directory: ./agent
        run: |
          go test -v ./...
          go vet ./...
          CGO_ENABLED=0 go build -v ./...
          TARGET_GLIBC_VERSION=2.34 bash agent/scripts/verify-linux-compat.sh bin/host-agent-linux-amd64

      # Repository lint
      - name: Repository lint
        run: |
          npx --yes prettier@3.5.3 --check ".github/**/*.{yml,yaml,md}" "docs/**/*.md" "frontend/src/**/*.{ts,tsx}"
          if grep -R -nE --exclude-dir=.git "^(<<<<<<<|=======|>>>>>>>)" .; then
            echo "Unresolved merge markers detected."
            exit 1
          fi

  # ============================================
  # Deploy - Run on push to main
  # ============================================
  deploy:
    name: Deploy
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    environment:
      name: production
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: agent/go.mod
          cache-dependency-path: agent/go.sum

      - name: Install frontend dependencies
        working-directory: ./frontend
        run: npm ci --legacy-peer-deps

      - name: Build deployment artifacts
        run: |
          npm --prefix frontend run build
          mvn -B -DskipTests package --file server/pom.xml
          mkdir -p agent/dist
          (cd agent && TARGET_GLIBC_VERSION=2.34 bash scripts/verify-linux-compat.sh dist/host-agent)

      - name: Package deployment bundle
        run: |
          mkdir -p dist/deploy/frontend
          cp -R frontend/dist/. dist/deploy/frontend/
          cp server/target/*.jar dist/deploy/
          cp agent/dist/host-agent dist/deploy/
          tar -czf production-bundle-${GITHUB_SHA}.tar.gz -C dist deploy

      - name: Upload deployment bundle
        uses: actions/upload-artifact@v4
        with:
          name: production-bundle
          path: production-bundle-${{ github.sha }}.tar.gz
          retention-days: 14

      - name: Record deployment summary
        run: |
          {
            echo "## Deployment"
            echo "- Branch: main"
            echo "- Commit: $GITHUB_SHA"
            echo "- Bundle: production-bundle-$GITHUB_SHA.tar.gz"
          } >> "$GITHUB_STEP_SUMMARY"

  # ============================================
  # Build Host Agent Packages - Run on push to main
  # ============================================
  build_host_packages:
    name: Build Host Agent Packages
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: pr_checks
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version-file: agent/go.mod
          cache-dependency-path: agent/go.sum

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: temurin
          cache: maven

      - name: Build Linux amd64 binary
        working-directory: ./agent
        run: |
          CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -o dist/host-agent-linux-amd64 ./cmd/host-agent
          TARGET_GLIBC_VERSION=2.34 bash agent/scripts/verify-linux-compat.sh dist/host-agent-linux-amd64

      - name: Build Windows amd64 binary
        working-directory: ./agent
        run: |
          CGO_ENABLED=0 GOOS=windows GOARCH=amd64 go build -trimpath -o dist/host-agent-windows-amd64.exe ./cmd/host-agent

      - name: Build macOS amd64 binary
        working-directory: ./agent
        run: |
          CGO_ENABLED=0 GOOS=darwin GOARCH=amd64 go build -trimpath -o dist/host-agent-macos-amd64 ./cmd/host-agent

      - name: Build macOS arm64 binary
        working-directory: ./agent
        run: |
          CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build -trimpath -o dist/host-agent-macos-arm64 ./cmd/host-agent

      - name: Create Linux package
        run: |
          mkdir -p packages/linux
          cp agent/dist/host-agent-linux-amd64 packages/linux/host-agent
          cp server/scripts/host-agent/install.sh packages/linux/
          cp server/scripts/host-agent/start.sh packages/linux/
          cp server/scripts/host-agent/stop.sh packages/linux/
          cp server/scripts/host-agent/update.sh packages/linux/
          chmod +x packages/linux/*
          tar -czf host-agent-linux-amd64.tar.gz -C packages/linux .

      - name: Create Windows package
        run: |
          mkdir -p packages/windows
          cp agent/dist/host-agent-windows-amd64.exe packages/windows/host-agent.exe
          cp server/scripts/host-agent/install.bat packages/windows/
          cp server/scripts/host-agent/start.bat packages/windows/
          cp server/scripts/host-agent/stop.bat packages/windows/
          cp server/scripts/host-agent/update.bat packages/windows/
          cd packages/windows && zip -r ../../host-agent-windows-amd64.zip .

      - name: Create macOS amd64 package
        run: |
          mkdir -p packages/macos-amd64
          cp agent/dist/host-agent-macos-amd64 packages/macos-amd64/host-agent
          cp server/scripts/host-agent/install.sh packages/macos-amd64/
          cp server/scripts/host-agent/start.sh packages/macos-amd64/
          cp server/scripts/host-agent/stop.sh packages/macos-amd64/
          cp server/scripts/host-agent/update.sh packages/macos-amd64/
          chmod +x packages/macos-amd64/*
          tar -czf host-agent-macos-amd64.tar.gz -C packages/macos-amd64 .

      - name: Create macOS arm64 package
        run: |
          mkdir -p packages/macos-arm64
          cp agent/dist/host-agent-macos-arm64 packages/macos-arm64/host-agent
          cp server/scripts/host-agent/install.sh packages/macos-arm64/
          cp server/scripts/host-agent/start.sh packages/macos-arm64/
          cp server/scripts/host-agent/stop.sh packages/macos-arm64/
          cp server/scripts/host-agent/update.sh packages/macos-arm64/
          chmod +x packages/macos-arm64/*
          tar -czf host-agent-macos-arm64.tar.gz -C packages/macos-arm64 .

      - name: Upload Linux package
        uses: actions/upload-artifact@v4
        with:
          name: host-agent-linux-amd64
          path: host-agent-linux-amd64.tar.gz
          retention-days: 1

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: host-agent-windows-amd64
          path: host-agent-windows-amd64.zip
          retention-days: 1

      - name: Upload macOS amd64 package
        uses: actions/upload-artifact@v4
        with:
          name: host-agent-macos-amd64
          path: host-agent-macos-amd64.tar.gz
          retention-days: 1

      - name: Upload macOS arm64 package
        uses: actions/upload-artifact@v4
        with:
          name: host-agent-macos-arm64
          path: host-agent-macos-arm64.tar.gz
          retention-days: 1

  # ============================================
  # Release - Run after deploy and build_host_packages
  # ============================================
  release:
    name: Release
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs:
      - deploy
      - build_host_packages
    steps:
      - name: Download deployment bundle
        uses: actions/download-artifact@v4
        with:
          name: production-bundle
          path: ./release-assets

      - name: Download Linux package
        uses: actions/download-artifact@v4
        with:
          name: host-agent-linux-amd64
          path: ./release-assets

      - name: Download Windows package
        uses: actions/download-artifact@v4
        with:
          name: host-agent-windows-amd64
          path: ./release-assets

      - name: Download macOS amd64 package
        uses: actions/download-artifact@v4
        with:
          name: host-agent-macos-amd64
          path: ./release-assets

      - name: Download macOS arm64 package
        uses: actions/download-artifact@v4
        with:
          name: host-agent-macos-arm64
          path: ./release-assets

      - name: Create GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: main-${{ github.run_number }}
          name: main-${{ github.run_number }}
          generate_release_notes: true
          files: |
            ./release-assets/production-bundle-${{ github.sha }}.tar.gz
            ./release-assets/host-agent-linux-amd64.tar.gz
            ./release-assets/host-agent-windows-amd64.zip
            ./release-assets/host-agent-macos-amd64.tar.gz
            ./release-assets/host-agent-macos-arm64.tar.gz

  # ============================================
  # Notify - Run after release
  # ============================================
  notify:
    name: Notify
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs:
      - deploy
      - release
    steps:
      - name: Publish workflow summary
        run: |
          {
            echo "## Post-merge Notifications"
            echo "- Deploy job: ${{ needs.deploy.result }}"
            echo "- Release job: ${{ needs.release.result }}"
            echo "- Notification time: $(date -u +"%Y-%m-%dT%H:%M:%SZ")"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Optional webhook notification
        env:
          NOTIFY_WEBHOOK_URL: ${{ secrets.NOTIFY_WEBHOOK_URL }}
        run: |
          if [ -z "$NOTIFY_WEBHOOK_URL" ]; then
            echo "NOTIFY_WEBHOOK_URL is not configured, skip external notification."
            exit 0
          fi

          payload=$(printf '{"text":"main merged and released: %s"}' "${GITHUB_SHA}")
          curl -sS -X POST \
            -H "Content-Type: application/json" \
            -d "$payload" \
            "$NOTIFY_WEBHOOK_URL"
